import { test } from 'node:test'
import assert from 'node:assert'

// 注:R6 集成测试需要 mock Anthropic.beta.files.create;本 test 占位标记重要性
test('placeholder — see R6 integration test in src/api/chat/__tests__/upload-integration.test.ts', () => {
  assert.ok(true)
})