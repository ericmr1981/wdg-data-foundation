# 实际执行记录（2026-03-22）

> 目的：把“初步验收”从纸面变成可复现的真实跑通记录。

- 目录：`/Users/ericmr/Documents/GitHub/Obsidian/项目/数据中台`
- PostgreSQL：Docker `postgres:16`（container: `dataplatform-pg`），DB=`dataplatform`，user/pass=`postgres/postgres`，port=`5432`

## 1) 启动 PostgreSQL（Docker）

```bash
docker run -d --name dataplatform-pg \
  -e POSTGRES_DB=dataplatform \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  postgres:16

docker exec dataplatform-pg pg_isready -U postgres -d dataplatform
```

## 2) 初始化 Schema + DDL（psycopg2 执行 SQL 文件）

说明：本机无 `psql` 时，使用 Python + psycopg2 执行 SQL 文件。

已执行顺序（新库）：
- `ops/OPS_DDL.sql`
- `sql/raw_ingest_file.sql`
- `brand-docs/Bonjur_ODS_DDL.sql`
- `sql/yufeng_apply_classification.sql`（已修复：函数返回 composite type）
- `sql/yufeng_coverage_and_unclassified.sql`
- `sql/yufeng_dm_models.sql`（已修复：month 字段别名）

并执行 Bonjur 兼容视图：
```sql
create or replace view bonjur_ods.sales_daily as
select * from bonjur_ods.sales_monthly;
```

## 3) 导入样例数据（真实导入）

### 3.1 Yufeng 银行流水导入
```bash
.venv/bin/python scripts/import_yufeng_bank_txn.py \
  inputs/yufeng/yf_gh/bank/2025-07/银行流水_工行_250301-250731.xlsx
```
关键结果：插入 `312` 行到 `yufeng_ods.bank_txn`（source_file_id=1）。

### 3.2 Bonjur 营业数据导入（mock）
```bash
.venv/bin/python scripts/import_bonjur_sales_daily.py \
  inputs/bonjur/wz_oh_wxc/sales/2026-02/mock_营业数据_自助营业取数_2026-02.csv
```
关键结果：插入 `2` 行到 `bonjur_ods.sales_monthly`（source_file_id=2）。

## 4) 一键 Pipeline（Yufeng）

```bash
.venv/bin/python scripts/run_pipeline_oneclick.py --brand yufeng
```

结果：`yufeng: success`

> 备注：此处曾发现 `check_yufeng_schema` 对 `yufeng_cfg.bank_rule_map` 用错 exists 类型检查（当 view 检查），已修复为 table 检查。

## 5) 只读核验（关键计数）

示例：
- `select count(*) from yufeng_ods.bank_txn;` → 312
- `select count(*) from yufeng_dm.v_bank_txn_classified;` → 312
- `select count(*) from yufeng_dm.revenue_monthly;` → 3
- `select count(*) from yufeng_dm.profit_monthly;` → 3

---

## 已知未清问题（不阻塞初步验收）

1) `ops_logger` 写 ops 表失败：`can't adapt type 'UUID'`（目前为 WARN，不阻塞主流程；后续需修复类型适配/字段类型）。
2) 覆盖率视图中部分金额字段显示 `NaN`：推测 `in_amt/out_amt` 列是 float 并引入 NaN 值；后续需把金额字段落为 numeric 并在导入时清洗 NaN。
