{
  "initCommand": "bash init.sh",
  "testCommand": "bash scripts/selftest.sh",
  "e2eCommand": "",
  "acceptanceSummary": "Repo-first harness: init, drift guard, and a lightweight selftest oracle (compileall + optional script entrypoint smoke tests when deps are installed)."
}
